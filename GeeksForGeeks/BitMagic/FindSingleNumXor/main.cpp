#include <stdio.h>

#define MAX 100

int main() {
  scanf("%d", &n);
  int sum = 0;
  int 3sum = 0;
  int once = 0;
  int tmp  = 0;

  for (int i = 0; i < n; i++) {
    scanf("%d", &tmp)
    if (tmp ^ once == 0) {

    } else {
      once ^= tmp;
    }
  }
}
