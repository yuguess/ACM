#include <stdio.h>
#include <iostream>
#include <string.h>
#include <vector>
#include <map>
using namespace std;

#define MAX 1010

char A[MAX];
char B[MAX];

int main() {
  int la = 0, lb = 0;
  scanf("%d %s", &la, A);
  scanf("%d %s", &lb, B);


}
